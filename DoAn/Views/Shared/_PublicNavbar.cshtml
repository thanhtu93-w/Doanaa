<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="@Url.Action("Index", "Home")">Gym Center</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Package")">Gói tập</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("AllClasses", "Home")">Lớp </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("AllTrainers", "Home")">Trainer</a>
                </li>
            </ul>

            <ul class="navbar-nav">
                @if (Session["UserName"] != null)
                {
                    <!-- Dropdown user -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                            @Session["UserName"]
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="@Url.Action("Profile", "Account")">Profile</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="@Url.Action("Logout", "Account")">Đăng xuất</a>
                            </li>
                        </ul>
                    </li>

                    <!-- Notification icon -->
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Notification")">
                            <i class="bi bi-bell"></i>
                        </a>
                    </li>
                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Login", "Account")">Đăng nhập</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Register", "Account")">Đăng ký</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>
